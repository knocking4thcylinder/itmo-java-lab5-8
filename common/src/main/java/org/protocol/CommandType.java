package org.protocol;

public enum CommandType {
    HELP,
    INFO,
    SHOW,
    INSERT,
    UPDATE,
    CLEAR,
    REMOVE_KEY,
    REPLACE_IF_LOWER,
    REMOVE_LOWER_KEY,
    REMOVE_GREATER_KEY,
    FILTER_BY_GENRE,
    FILTER_CONTAINS_NAME,
    FILTER_LESS_THAN_MPAA_RATING,
}
